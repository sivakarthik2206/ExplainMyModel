# ExplainMyModel
A Streamlit + FastAPI web app that converts any trained ML model into clear, human-readable explanations using SHAP. Upload a model and CSV → get feature importance plots, natural-language insights, and improvement suggestions. Project showcasing end-to-end ML explainability, backend APIs, and clean UI/UX.
